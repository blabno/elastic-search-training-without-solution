module.exports = function(esUrl,index) {
    return {
        getOrdersCountAndIncomePerDay: function(year,month) {
        //for each day in given month show number of orders created and sum of those orders value
        }
    }
};
