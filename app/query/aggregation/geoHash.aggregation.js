module.exports = function(esUrl,index) {
    return {
        groupCustomersPerArea: function(precision) {
        //aggregate per geohash of specified precision, results will be used in heatmap
        }
    }
};
