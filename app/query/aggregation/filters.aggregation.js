module.exports = function(esUrl,index) {
    return {
        getNewCustomerCountsEachDay: function(year,month) {
        //This should return separate aggregation for VIP and non-VIP customers
        }
    }
};
