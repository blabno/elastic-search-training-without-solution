module.exports = function(esUrl,index) {
    return {
        getCustomersAgeReport: function() {
        //tell me how many customers in specific age range we've got, range size should be 5, this should be for barchart
        }
    }
};
